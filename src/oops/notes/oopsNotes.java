package oops.notes;

public class oopsNotes {

    //public = same class, same package, diff. package
//   private = same class
//   protected =  same class, same package,child class
//   dafault = same class, same package

    // private constructor = if the constructor is private we can object inside the same class and not from another class.

    // singleton class= A class which we can create only one object

    // constructor = special method is used to initialize objects and create instance of a class
    /*  eg.  class Demo1{
             int age;
             String name;
             public Demo1(int age, String name) {
             }      }
    * */

}
